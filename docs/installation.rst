============
Installation
============

At the command line::

    $ easy_install django-desktopapp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-desktopapp
    $ pip install django-desktopapp
